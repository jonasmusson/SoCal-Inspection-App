# SoCal Autoworks Inspection App

Private production source for the SoCal Autoworks vehicle inspection and customer reporting application.

## Development

- `npm ci`
- `npm run typecheck`
- `npm run build`

Supabase migrations and Edge Functions are stored in `supabase/`. Local environment values are intentionally excluded from Git.
